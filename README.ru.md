# Crypto Wallets Service

Высокопроизводительный и безопасный микросервис для генерации, хранения и подписи криптовалютных транзакций. Написан на Rust с использованием Axum, PostgreSQL и HashiCorp Vault.

## 🚀 Возможности

*   **Мульти-чейн**: поддержка Ethereum (ETH), Tron (TRX), Solana (SOL), TON.
*   **Безопасность**:
    *   Мнемонические фразы (seed) шифруются через **Transit Engine** в HashiCorp Vault.
    *   Ключи в открытом виде никогда не хранятся в БД.
*   **HD Wallets**: Иерархическая детерминированная генерация (BIP-32/39/44).
*   **Наблюдаемость (Observability)**:
    *   Метрики Prometheus (`/metrics`).
    *   Структурированное логирование с `X-Request-ID`.
*   **Документация**:
    *   Swagger UI (`/swagger-ui`).
    *   Коллекция Postman в комплекте.
*   **Надежность**:
    *   Rate Limiting (ограничение запросов).
    *   Аудит действий (Audit Logs).
    *   Полноценные интеграционные тесты (Testcontainers).

## 🛠 Архитектура

*   **API Layer**: Rust (Axum) - Обработка HTTP/gRPC.
*   **Storage**: PostgreSQL - Хранение зашифрованных метаданных и путей деривации.
*   **Key Management**: HashiCorp Vault - Шифрование и расшифровка на лету.

## 🏁 Начало работы

### Требования

*   Rust 1.80+
*   Docker & Docker Compose

### Локальный запуск

1.  **Запуск инфраструктуры**:
    ```bash
    docker-compose up -d
    ```
    Поднимет Postgres (5432) и Vault (8200).

2.  **Запуск сервиса**:
    ```bash
    cargo run
    ```
    Сервис будет доступен по адресу `0.0.0.0:3000`.

### Конфигурация

Настройка через `config/default.toml` или переменные окружения.

| Переменная | По умолчанию | Описание |
| :--- | :--- | :--- |
| `SERVER.HOST` | `127.0.0.1` | Адрес прослушивания |
| `SERVER.PORT` | `3000` | Порт |
| `DATABASE.URL` | `postgres://...` | Строка подключения к БД |
| `VAULT.URL` | `http://127.0.0.1:8200` | Адрес Vault |
| `VAULT.TOKEN` | `root` | Токен Vault |

## 📖 API Документация

*   **Swagger UI**: Откройте `http://localhost:3000/swagger-ui` в браузере.
*   **Health Check**: `GET /api/v1/health`
*   **Создание кошелька**: `POST /api/v1/wallets`
*   **Подпись транзакции**: `POST /api/v1/wallets/{id}/sign`

## 👨‍💻 Руководство разработчика

### Добавление новой сети

1.  **Enum**: Добавьте сеть в `src/core/mod.rs` (`enum Network`).
2.  **Деривация**: Реализуйте путь (derivation path) в `src/core/wallet.rs`.
3.  **Формат адреса**: Реализуйте кодирование адреса в `src/core/address.rs`.
4.  **Подпись**: Добавьте логику подписи в функцию `sign_tx`.

### Тестирование

*   **Unit тесты**:
    ```bash
    cargo test --lib
    ```
*   **Интеграционные тесты**:
    Используют Docker для поднятия реального окружения.
    ```bash
    cargo test --test integration_test
    ```

## 📦 Деплой

### Docker

Сборка продакшн образа:
```bash
docker build -f docker/prod.Dockerfile -t crypto-wallets-service:latest .
```

Запуск:
```bash
docker run -p 3000:3000 -e DATABASE_URL=... -e VAULT_URL=... crypto-wallets-service:latest
```

## 🔒 Безопасность

*   В продакшене используйте HTTPS.
*   Регулярно ротируйте `VAULT_TOKEN`.
*   Настоятельно рекомендуется использовать закрытый контур сети для доступа к БД и Vault.

## 📄 Лицензия

MIT
